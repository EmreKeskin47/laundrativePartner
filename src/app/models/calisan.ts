export class Calisan {
  isim: string;
  email: string;
  tel: string;

  constructor(isim: string, email: string, tel: string) {
    this.isim = isim;
    this.email = email;
    this.tel = tel;
  }
}
